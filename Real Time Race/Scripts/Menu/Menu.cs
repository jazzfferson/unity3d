using UnityEngine;
using System.Collections;

public class Menu : MonoBehaviour {

   /* public Popup pista, carro;

    void Start()
    {
        carro.items.Add(Ecar.GTOne.ToString());
        carro.items.Add(Ecar.OldCar.ToString());
        carro.items.Add(Ecar.Mustang.ToString());

        pista.items.Add(Epista.TestTrack.ToString());
        pista.items.Add(Epista.SpenMotorsport.ToString());
        pista.items.Add(Epista.Montain.ToString());


    }
    public void ExitGame()
    {
        Application.Quit();
    }
    public void StartGame()
    {

        

        for(int i = 0 ; i < carro.items.Count;i++)
        {
            if(carro.items[i] == carro.value)
            {
                RacePrepare.carro = (Ecar)i;
                break;
            }
        }

         for(int i = 0 ; i < pista.items.Count;i++)
        {
            if(pista.items[i] == pista.value)
            {
                RacePrepare.pista = (Epista)i;
                break;
            }
        }


         LoadScreen.instance.LoadLevel(RacePrepare.pista.ToString());
    }*/
}
